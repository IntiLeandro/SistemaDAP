@model IEnumerable<SistemaDAP.Models.banco>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_banco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_banco)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_banco }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_banco }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_banco })
        </td>
    </tr>
}

</table>
