@model IEnumerable<SistemaDAP.Models.tipo_servicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_tipo_servicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_tipo_servicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_servicio }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_servicio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_servicio })
        </td>
    </tr>
}

</table>
