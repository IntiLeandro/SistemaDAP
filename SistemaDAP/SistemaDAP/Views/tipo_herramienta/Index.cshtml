@model IEnumerable<SistemaDAP.Models.tipo_herramienta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_tipo_herramienta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_tipo_herramienta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_herramienta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_herramienta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_herramienta })
        </td>
    </tr>
}

</table>
